# Build le projet
npm run build

# Initialisez git dans dist
cd dist
git init
git add -A
git commit -m "deploy"
git branch -M gh-pages
git remote add origin https://github.com/Florian-Glay/portfolio.git

git remote -v
git fetch origin
git ls-remote origin gh-pages
git push --force origin gh-pages